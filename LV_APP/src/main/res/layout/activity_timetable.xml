<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#f5f5f5"
    tools:context=".TimetableFragment">

    <!-- 요일 헤더 (월,화,수,목,금) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@android:color/white"
        android:elevation="2dp">

        <TextView style="@style/TimetableHeader" android:text="월"/>
        <TextView style="@style/TimetableHeader" android:text="화"/>
        <TextView style="@style/TimetableHeader" android:text="수"/>
        <TextView style="@style/TimetableHeader" android:text="목"/>
        <TextView style="@style/TimetableHeader" android:text="금"/>
    </LinearLayout>

    <!-- 수업 목록이 표시될 RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_timetable"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="5"
        tools:listitem="@layout/item_class"/>

    <!-- 수업 추가 버튼 -->
    <Button
        android:id="@+id/button_add_class"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_margin="16dp"
        android:text="수업 추가"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#ffffff"
        android:backgroundTint="#0ea5e9"
        app:cornerRadius="12dp"
        app:icon="@drawable/ic_plus"
        app:iconGravity="textStart"/>

</LinearLayout>
