<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="#E5E7EB"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="20dp">

        <!-- 수업 이름 -->
        <TextView
            android:id="@+id/tv_class_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="#1E293B"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/tv_date"
            android:layout_marginEnd="16dp"
            tools:text="웹 프로그래밍" />

        <!-- 날짜 -->
        <TextView
            android:id="@+id/tv_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#6B7280"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_class_name"
            app:layout_constraintBottom_toBottomOf="@id/tv_class_name"
            tools:text="2025-11-17" />

        <!-- 구분선 -->
        <View
            android:id="@+id/divider_record"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="#E5E7EB"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/tv_class_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- 출석 현황 (현재/총원/결석) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="16dp"
            android:baselineAligned="false"
            app:layout_constraintTop_toBottomOf="@id/divider_record"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- 현재 인원 -->
            <LinearLayout style="@style/RecordItem">
                <TextView style="@style/RecordItem.Title" android:text="출석"/>
                <TextView style="@style/RecordItem.Value" android:id="@+id/tv_present_count" tools:text="28"/>
            </LinearLayout>

            <!-- 총 인원 -->
            <LinearLayout style="@style/RecordItem">
                <TextView style="@style/RecordItem.Title" android:text="총원"/>
                <TextView style="@style/RecordItem.Value.Dark" android:id="@+id/tv_total_count" tools:text="30"/>
            </LinearLayout>

            <!-- 결석 -->
            <LinearLayout style="@style/RecordItem">
                <TextView style="@style/RecordItem.Title" android:text="결석"/>
                <TextView style="@style/RecordItem.Value.Red" android:id="@+id/tv_absent_count" tools:text="2"/>
            </LinearLayout>
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
